# Local development config
CLAUDE.md

# Source reference materials (large, binary or derived)
resources/*.pdf
resources/*.txt

# Generated code — tracked for now so the repo is cloneable and runnable.
# The system model (sys/) is authoritative. When blueprint can self-host,
# src/ may become gitignored and regenerated as a build step.
# src/
