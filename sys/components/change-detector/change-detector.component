component ChangeDetector
  responsibility: Compare the current system model against the last
                  synced state and identify which components are
                  affected by the changes — through direct modification
                  or through changed dependencies (requirements,
                  interfaces, test specifications).
  depends_on: Parser
  satisfies: BP-007
