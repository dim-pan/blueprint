component Validator
  responsibility: Check that the parsed system model is internally
                  consistent — all cross-references resolve, all
                  required fields are present, and no contradictions
                  exist. Report each error with source file and
                  location.
  depends_on: Parser
  satisfies: BP-002, BP-003
